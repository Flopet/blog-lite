services:
  caddy:
    image: caddy:2-alpine
    container_name: blog-lite
    restart: unless-stopped
    ports:
      - "3939:80"
    volumes:
      - ${APPDATA_PATH}caddy/Caddyfile:/etc/caddy/Caddyfile:ro
      - ${APPDATA_PATH}site:/srv:ro
      - ${APPDATA_PATH}caddy/data:/data
      - ${APPDATA_PATH}caddy/config:/config
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost/"]
      interval: 2s
      timeout: 5s
      retries: 3
      start_period: 3s

